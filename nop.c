#include "monty.h"

/**
 * op_nop - fxn that do nothing
 * @head: pointer to the pointer to the head of the stack
 * @linenum: current line number
 *
 * Return: nothing
 */
void op_nop(stack_t **head, unsigned int linenum)
{
	(void)head;
	(void)linenum;
}
